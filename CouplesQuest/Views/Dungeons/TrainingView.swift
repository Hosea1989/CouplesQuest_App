import SwiftUI

// Old gold-for-stat TrainingView has been removed.
// Training is now the AFK system (formerly Missions) accessed via AdventuresHubView.
// This file is kept as a stub to avoid project file issues.

struct TrainingView: View {
    var body: some View {
        EmptyView()
    }
}

#Preview {
    TrainingView()
}
